# Generated by scripts/build_openapi.py from src/server/api_catalog.py.
# Do not edit by hand — edit the catalog and regenerate.
openapi: 3.1.0
info:
  title: HPC Status Monitor API
  description: 'Read-only JSON over plain HTTP, no authentication, CORS open to any origin.
    Every panel in the dashboard is backed by one of these endpoints.


    The same catalog is served live at /api/endpoints and rendered at /api.html.


    See docs/glossary.md for terminology.'
  version: 2.1.0
  license:
    name: MIT
servers:
- url: http://localhost:8080
  description: Local development server
- url: '{protocol}://{host}:{port}{prefix}'
  description: Configurable deployment
  variables:
    protocol:
      default: http
      enum:
      - http
      - https
    host:
      default: localhost
    port:
      default: '8080'
    prefix:
      default: ''
      description: URL prefix for reverse-proxy deployments
tags:
- name: Fleet
  description: Fleet-wide status, topology and history
- name: Clusters
  description: Per-cluster queues, allocations and storage
- name: Decisions
  description: Placement rankings and generated insights
- name: Reference
  description: Configuration and self-description
- name: Control
  description: Operations that change server state
paths:
  /api/status:
    get:
      tags:
      - Fleet
      summary: Every system in the fleet, with its status
      description: 'The collector''s raw payload: one row per system plus fleet-wide counts.
        This is what the Fleet status page renders.


        Answers 503 with an explanation while the first collection is still running.'
      operationId: apiStatus
      responses:
        '200':
          description: Every system in the fleet, with its status
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta: {}
                  summary: {}
                  systems: {}
  /api/fleet/summary:
    get:
      tags:
      - Fleet
      summary: Condensed fleet listing
      description: The same systems, trimmed to the fields a table needs.
      operationId: apiFleetSummary
      responses:
        '200':
          description: Condensed fleet listing
          content:
            application/json:
              schema:
                type: object
                properties:
                  generated_at: {}
                  fleet_stats: {}
                  systems: {}
  /api/topology:
    get:
      tags:
      - Fleet
      summary: 'The fleet as a graph: monitor, sites, systems'
      description: Merges what a site reports about a machine with what the monitor observes
        by connecting to it. Always answers 200 — a fleet that has not been collected yet
        returns just the monitor node with meta.ready false.
      operationId: apiTopology
      responses:
        '200':
          description: 'The fleet as a graph: monitor, sites, systems'
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta: {}
                  summary: {}
                  sites: {}
                  nodes: {}
                  edges: {}
  /api/history:
    get:
      tags:
      - Fleet
      summary: Replayable frames of status and utilization
      description: Status transitions carried forward and queue depth bucketed onto evenly
        spaced instants — what the topology timeline plays back.
      operationId: apiHistory
      responses:
        '200':
          description: Replayable frames of status and utilization
          content:
            application/json:
              schema:
                type: object
                properties:
                  window_hours: {}
                  step_minutes: {}
                  from: {}
                  to: {}
                  systems: {}
                  frames: {}
      parameters:
      - name: window
        in: query
        required: false
        description: Hours to replay
        schema:
          type: integer
          default: 24
      - name: step
        in: query
        required: false
        description: Minutes between frames
        schema:
          type: integer
          default: 15
  /api/events:
    get:
      tags:
      - Fleet
      summary: Recent system state changes
      description: The transition log behind alerting, newest first. Populated whether or
        not a webhook is configured.
      operationId: apiEvents
      responses:
        '200':
          description: Recent system state changes
          content:
            application/json:
              schema:
                type: object
                properties:
                  events: {}
                  alerting_enabled: {}
                  generated_at: {}
      parameters:
      - name: limit
        in: query
        required: false
        description: Maximum events (1-200)
        schema:
          type: integer
          default: 50
  /api/cluster-usage:
    get:
      tags:
      - Clusters
      summary: Queues, nodes, allocations and storage per cluster
      description: 'Everything the monitor collects over its live sessions. Each queue also
        carries a wait_estimate once there is enough recorded history to derive one.


        While the first sweep runs this returns an envelope with status ''warming_up'' or
        ''partial'' and a progress object, rather than an error.'
      operationId: apiClusterUsage
      responses:
        '200':
          description: Queues, nodes, allocations and storage per cluster
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    cluster_metadata: {}
                    usage_data: {}
                    queue_data: {}
                    gpu_data: {}
                    system_info: {}
                    storage_data: {}
  /api/cluster-usage/{cluster}:
    get:
      tags:
      - Clusters
      summary: One cluster's profile
      description: The same data for a single cluster, keyed by its slug.
      operationId: apiClusterUsageCluster
      responses:
        '200':
          description: One cluster's profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  cluster: {}
                  slug: {}
                  usage: {}
                  queues: {}
                  node_classes: {}
                  placement_hint: {}
      parameters:
      - name: cluster
        in: path
        required: true
        description: Slug identifying the cluster
        schema:
          type: string
  /api/storage:
    get:
      tags:
      - Clusters
      summary: Filesystem capacity and usage
      description: Storage for every connected cluster, sharing the warming-up envelope.
      operationId: apiStorage
      responses:
        '200':
          description: Filesystem capacity and usage
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    cluster_metadata: {}
                    storage_data: {}
  /api/placement:
    get:
      tags:
      - Decisions
      summary: Rank queues for a job
      description: Scores every queue that can run the job on idle capacity, measured wait,
        backlog, and allocation headroom. Queues that cannot run it come back under 'blocked'
        with the reason.
      operationId: apiPlacement
      responses:
        '200':
          description: Rank queues for a job
          content:
            application/json:
              schema:
                type: object
                properties:
                  request: {}
                  candidates: {}
                  blocked: {}
                  considered: {}
                  generated_at: {}
      parameters:
      - name: cores
        in: query
        required: false
        description: Cores the job needs
        schema:
          type: integer
          default: 1
      - name: hours
        in: query
        required: false
        description: Walltime in hours
        schema:
          type: number
          default: 1
      - name: gpus
        in: query
        required: false
        description: GPUs the job needs
        schema:
          type: integer
          default: 0
      - name: queue_type
        in: query
        required: false
        description: Restrict to a queue type or name
        schema:
          type: string
      - name: limit
        in: query
        required: false
        description: Maximum candidates
        schema:
          type: integer
          default: 5
  /api/insights:
    get:
      tags:
      - Decisions
      summary: Generated warnings and recommendations
      description: Allocation, backlog, GPU and status findings, sorted by priority.
      operationId: apiInsights
      responses:
        '200':
          description: Generated warnings and recommendations
          content:
            application/json:
              schema:
                type: object
                properties:
                  insights: {}
                  generated_at: {}
  /api/system-markdown/{system}:
    get:
      tags:
      - Reference
      summary: A system's briefing
      description: Markdown notes scraped for a system, keyed by slug.
      operationId: apiSystemMarkdownSystem
      responses:
        '200':
          description: A system's briefing
          content:
            application/json:
              schema:
                type: object
                properties:
                  slug: {}
                  content: {}
      parameters:
      - name: system
        in: path
        required: true
        description: Slug identifying the system
        schema:
          type: string
  /api/config:
    get:
      tags:
      - Reference
      summary: Deployment configuration the frontend needs
      description: Branding, enabled tabs, feature flags and topology settings. Secrets are
        never included — the alert webhook URL in particular.
      operationId: apiConfig
      responses:
        '200':
          description: Deployment configuration the frontend needs
          content:
            application/json:
              schema:
                type: object
                properties:
                  deployment: {}
                  ui: {}
                  features: {}
                  topology: {}
  /api/endpoints:
    get:
      tags:
      - Reference
      summary: This catalog
      description: Describes every endpoint, including itself.
      operationId: apiEndpoints
      responses:
        '200':
          description: This catalog
          content:
            application/json:
              schema:
                type: object
                properties:
                  endpoints: {}
                  groups: {}
                  base_url: {}
                  generated_at: {}
  /api/v2/collectors/status:
    get:
      tags:
      - Reference
      summary: Whether each collector has data yet
      description: Readiness per collector, for health checks and debugging.
      operationId: apiV2CollectorsStatus
      responses:
        '200':
          description: Whether each collector has data yet
          content:
            application/json:
              schema:
                type: object
                properties:
                  collectors: {}
  /app-config.js:
    get:
      tags:
      - Reference
      summary: Frontend configuration as a script
      description: The same configuration as /api/config, delivered as JavaScript that assigns
        window.APP_CONFIG. Not JSON.
      operationId: app-config-js
      responses:
        '200':
          description: Frontend configuration as a script
          content:
            application/javascript:
              schema:
                type: string
  /api/refresh:
    post:
      tags:
      - Control
      summary: Collect from source now
      description: Runs a fleet refresh synchronously and returns whether it succeeded. No
        request body.
      operationId: apiRefresh
      responses:
        '200':
          description: Collect from source now
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: {}
                  detail: {}
